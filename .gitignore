# Local scratch: proxy captures and working notes.
*.chlz
*.chls

brightwheel-checkin-shortcut-handoff.md

# dist/ is intentionally NOT ignored. The generated .xml and signed .shortcut
# are committed so an installable build is always available from the repo, and
# so the XML diff shows what a generator change actually did.
